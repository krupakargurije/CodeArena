spring:
  application:
    name: codearena-backend
  
  datasource:
    # Supabase Direct Connection (Session Mode) - Required for DDL/Migrations
    url: ${DB_URL:jdbc:postgresql://db.qlwrjoknqjwurgnoogjd.supabase.co:5432/postgres?sslmode=require}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:Nobita@2004}
    driver-class-name: org.postgresql.Driver
  
  jpa:
    hibernate:
      ddl-auto: ${DDL_AUTO:update}
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: ${PORT:8080}   # Render sets PORT automatically
  address: 0.0.0.0     # REQUIRED for Render – binds to all network interfaces
  error:
    include-message: always
    include-binding-errors: always

# JWT Configuration
jwt:
  secret: "8OI/UlECnMWScADoBwzUaoAvloaK46/RJEIS5EBFUdnn8QjoGU4rcbSdMbiOlqsLsqk3xVi/ZzLQ1F9BP3k3xQ=="
  expiration: 86400000 # 24 hours in milliseconds

# CORS Configuration
cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:5173}
  allowed-methods: GET,POST,PUT,DELETE,OPTIONS
  allowed-headers: "*"
  allow-credentials: true

# Code Execution Configuration
code-execution:
  timeout: 5000 # 5 seconds
  memory-limit: 256 # MB

# Supabase Configuration
# IMPORTANT: Using service_role key for admin operations (bypasses RLS)
# WARNING: Never expose this key to the frontend
supabase:
  url: https://qlwrjoknqjwurgnoogjd.supabase.co
  key: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InFsd3Jqb2tucWp3dXJnbm9vZ2pkIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc2NDUwNzg5OSwiZXhwIjoyMDgwMDgzODk5fQ.VDwp7I70sZFYH2ubslzCBI6nv0JJq41bmK4TPV0erKU
